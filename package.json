{
  "dependencies": {
    "tailwindcss-filters": "^3.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.1.0",
    "laravel-mix": "^6.0.0-beta.15",
    "postcss-import": "^13.0.0",
    "tailwindcss": "^2.0.1"
  },
  "scripts": {
    "development": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "hot": "mix watch --hot",
    "production": "mix --production",
    "blade": "blade-formatter --write resources/**/*.blade.php"
  }
}
